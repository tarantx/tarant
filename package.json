{
  "name": "@wind-js/actor",
  "version": "2.0.3",
  "description": "Actor model for reactive and scalable applications.",
  "main": "dist/index.js",
  "private": false,
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"**/*.ts\" && tslint -p tsconfig.json --fix lib/**/*.ts -t verbose",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jestconfig.json",
    "test:coverage": "jest --config jestconfig.json --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js"
  },
  "contributors": [
    "Kevin Mas Ruiz <masruizkevin@gmail.com>"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/uuid": "^3.4.4",
    "coveralls": "^3.0.2",
    "jest": "^23.6.0",
    "prettier": "^1.15.2",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.16.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "uuid": "^3.3.2"
  }
}
